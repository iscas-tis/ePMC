package epmc.expression.standard;

/**
 * Type of an expression.
 * This is not the same as the values from the value package, but rather an
 * abstract type. It should be evaluated to a concrete type in in evaluators.
 * 
 * @author Ernst Moritz Hahn
 */
public interface ExpressionType {
}
