package epmc.imdp.operator;

import epmc.operator.Operator;

/**
 * Operator to construct an interval from two real numbers.
 * 
 * @author Ernst Moritz Hahn
 */
public enum OperatorInterval implements Operator {
    INTERVAL
}
